import React from 'react';
import { Link } from 'react-router-dom';

const Navigation = () => (
  <nav className="nav">
    <Link to='/'                 className="nav-link">Maintain Departments</Link>
  </nav>
)

export default Navigation
